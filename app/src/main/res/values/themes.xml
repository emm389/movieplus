<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MoviePlus" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
        <item name="colorPrimaryVariant">@color/purple_200</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.MoviePlus.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MoviePlus.AppBarOverlay" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:background">@color/black</item>
    </style>

    <style name="Theme.MoviePlus.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.TabStyle" parent="Widget.Design.TabLayout">
        <item name="tabSelectedTextColor">@color/purple_200</item>
        <item name="tabBackground">@color/black</item>
        <item name="tabIndicatorColor">@color/purple_200</item>
        <item name="tabIndicatorHeight">3dp</item>
        <item name="tabMode">fixed</item>
    </style>

    <style name="Theme.MoviePlus.CustomTextAppearance" parent="TextAppearance.Design.CollapsingToolbar.Expanded">
        <item name="android:textSize">25sp</item>
        <item name="fontFamily">sans-serif-black</item>
    </style>
</resources>