<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.emmanuel.movieplus">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".MovieApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_movie_plus"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/circular_movie_plus"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoviePlus">

        <activity
            android:name=".movies.MoviesActivity"
            android:exported="true"
            android:theme="@style/Theme.MoviePlus.NoActionBar"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>